bin/
dist/
/mmk
*.db
*.db-journal
.superpowers/
.env
.worktrees/
mmk-debug.log

# HAR captures of the Slack client. These carry live xoxc/xoxd
# credentials and real message content, so they must never be
# committed. Sanitized extracts belong in testdata/ instead.
*.har
